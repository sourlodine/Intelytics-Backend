const getTokenData = require('./getTokenData');
const getCurrentPrice = require('./getCurrentPrice');
const getDataByInterval = require('./getDataByInterval');

module.exports ={
    getTokenData,
    getCurrentPrice,
    getDataByInterval,
}